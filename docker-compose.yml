# Local development: MongoDB instance
# Usage: docker compose up -d
# Connect with: mongodb://localhost:27017/son_of_steve

services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

volumes:
  mongo-data:
