# Auto-generated by demiurge — do not edit manually
# Override by placing a demiurge.yaml in your repo root
version: 1

app:
  type: fullstack
  root_url: http://localhost:3000
  api_url: http://localhost:3000/api

services:
  compose-docker-compose.yml:
    kind: api
    startup_mode: compose
    compose_target: compose-docker-compose.yml
    ports:
      - container: 3000
        host: 3000
    readiness:
      probe_type: http
      target: http://localhost:3000/
      interval_ms: 1000
      timeout_ms: 3000
      max_failures: 10
    required: true

verification:
  default_verifier_timeout_ms: 30000
  default_browser_action_timeout_ms: 15000
  max_retries: 1
  retry_delay_ms: 1000
  screenshot_on_failure: false
  screenshot_on_complete: false
  trace_enabled: false

inference:
  default_provider: mock

policies:
  max_attempts: 5
  run_timeout_ms: 3600000
  attempt_timeout_ms: 900000
  max_patch_lines: 2000
  max_artifact_disk_bytes: 536870912
